fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pyyaml==6.0.2
python-multipart==0.0.9
pytest==8.3.3
httpx==0.27.2

# Day 2: document ingestion / OCR
pymupdf==1.24.11
pytesseract==0.3.13
pillow==10.4.0

# Day 6: chunking + embeddings + vector search
# chromadb==0.5.20 originally, bumped to 1.0.15 on Day 6 (see MISTAKES.md):
# 0.5.20 pins chroma-hnswlib==0.7.6, a version with NO prebuilt wheel for
# any platform, forcing a from-source C++ build that fails on Windows
# without Visual C++ Build Tools installed. 1.0.15 ships its own compiled
# backend as a single prebuilt wheel and has no compile-from-source step.
chromadb==1.0.15

# Dev-only: generates synthetic sample/golden documents for local testing.
# Not needed to run the API itself.
reportlab==4.2.5

# Day 13: monitoring
prometheus-client==0.21.0
